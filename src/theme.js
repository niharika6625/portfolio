//TODO make changes to the theme colors
import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#42446e',
    },
    secondary: {
      main: '#e2ba5a',
    },
    background: {
      default: '#e0e0f2',
    },
  },
});
